import { defineTool } from "./defineTool-77H4nSna.js";
import { apiServiceFromContext } from "./api-utils-BlUSO8xh.js";

//#region src/tools/whoami.ts
var whoami_default = defineTool({
	name: "whoami",
	description: [
		"Identify the authenticated user in Sentry.",
		"",
		"Use this tool when you need to:",
		"- Get the user's name and email address."
	].join("\n"),
	inputSchema: {},
	async handler(params, context) {
		const apiService = apiServiceFromContext(context);
		const user = await apiService.getAuthenticatedUser();
		return `You are authenticated as ${user.name} (${user.email}).\n\nYour Sentry User ID is ${user.id}.`;
	}
});

//#endregion
export { whoami_default };
//# sourceMappingURL=whoami-Bswhb72a.js.map