const require_defineTool = require('./defineTool-lWNTzWl_.cjs');
const require_api_utils = require('./api-utils-DFWUfQ83.cjs');

//#region src/tools/whoami.ts
var whoami_default = require_defineTool.defineTool({
	name: "whoami",
	description: [
		"Identify the authenticated user in Sentry.",
		"",
		"Use this tool when you need to:",
		"- Get the user's name and email address."
	].join("\n"),
	inputSchema: {},
	async handler(params, context) {
		const apiService = require_api_utils.apiServiceFromContext(context);
		const user = await apiService.getAuthenticatedUser();
		return `You are authenticated as ${user.name} (${user.email}).\n\nYour Sentry User ID is ${user.id}.`;
	}
});

//#endregion
Object.defineProperty(exports, 'whoami_default', {
  enumerable: true,
  get: function () {
    return whoami_default;
  }
});
//# sourceMappingURL=whoami-BOJHfdih.cjs.map